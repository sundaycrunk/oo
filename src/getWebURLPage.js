import React, { Component } from 'react'

export default class getWebURLPage extends Component {
  constructor(props) {
      super(props)

      this.state = {
           weburl: props.fuck
      }
  }

  // i realise it's gonna have to be a functional component
  // which does not return anything.

    render() {
        return (
            <div>

            </div>
        )
    }
}
